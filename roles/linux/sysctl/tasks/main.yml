- name: Ensure sysctl dir exists
  ansible.builtin.file:
    path: /etc/sysctl.d
    state: directory
    mode: "0755"

- name: Deploy sysctl configuration
  ansible.builtin.copy:
    src: files/sysctl.conf
    dest: /etc/sysctl.d/99-tuned-network.conf
    mode: "0644"
  notify: Reload sysctl
